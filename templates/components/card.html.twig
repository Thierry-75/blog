<div class="card mt-1 mb-1" style="width: 24rem;">
	<img src="https://tecdn.b-cdn.net/img/new/standard/nature/186.jpg" class="card-img-top" alt="...">
	<div class="card-body">
		<h5 class="card-title">{{ article.title }}</h5>
		<p class="card-text">{{ article.content | u.truncate(120,'...') }}</p>
		<a href="#" class="btn btn-primary">En savoir plus</a>
	</div>
    <div class="card-footer text-body-secondary text-sm-center">
    Mise à jour le: {{ article.createdAt | date('d/m/Y') }}
  </div>
</div>
